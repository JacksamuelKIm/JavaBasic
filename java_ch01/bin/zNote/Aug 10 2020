오늘 배운내용(역순)

1. Stream이라는 게 도대체 뭐야?
   
   Stream = a sequence of data / 일련의 데이터 / 
   InputStream 한 소스(source)로부터 데이터를 읽을 때(read) 쓰이는 추상(abstract)클래스   
   OutputStream 대상(destination)에 쓸 때(write) 사용하는 추상(abstract)클래스

     
   https://www.tutorialspoint.com/java/java_files_io.htm#:~:text=As%20described%20earlier%2C%20a%20stream,with%20Input%20and%20Output%20streams.
   

   
	1-1. getResourceAsStream(String name )
	  String 자리에 읽어들일 파일 경로를 제대로 써주면 
	  잘 읽어드림  